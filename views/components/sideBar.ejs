<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>sideBar</title>

    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <!-- Inter Font -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@600&display=swap" rel="stylesheet" />
    <!-- Estilos customizados -->
    <link rel="stylesheet" href="/assets/css/index.css" />

    <style>
        body {
            margin: 0;
            display: flex;
            font-family: sans-serif;
        }
    </style>
</head>

<body>


    <div class="sidebar">
        <%- include('./userCard', { userName: 'Admin 1' , userEmail: 'admin1@ufc.br' }) %>

        <div class="mt-3">
            <%- include('./sideBarButton', { iconPath: `/assets/imgs/dashboard.svg`, buttonText: 'Dashboard'
                    }) %>
            <%- include('./sideBarButton', { iconPath: `/assets/imgs/upload.svg`, buttonText: 'Historico de Uploads'
                    }) %>
            <%- include('./sideBarButton', { iconPath: `/assets/imgs/sistema.svg`, buttonText: 'Estatísticas do Sistema'
                    }) %>
        </div>


        <div class="mt-auto">
            <hr class="my-3">

            <%- include('./sideBarButton', { iconPath: `/assets/imgs/tema.svg`, buttonText: 'Trocar para Modo Claro'
                }) %>
            <%- include('./sideBarButton', { iconPath: `/assets/imgs/novaGuia.svg`, buttonText: 'Updates & FAQ'
                }) %>
            <%- include('./sideBarButton', { iconPath: `/assets/imgs/logOff.svg`, buttonText: 'Sair', textColor: '#FF5555'
                }) %>
        </div>

    </div>

    <div class="conteudo">
        <h1>Conteúdo principal</h1>
        <p>Texto aqui...</p>
    </div>
</body>

</html>